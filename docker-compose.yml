services:
  db:
    image: postgres:16-alpine
    container_name: sistema-controle-despesas-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-sistema_despesas}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-sistema_despesas}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sistema-despesas-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sistema-controle-despesas-app
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-sistema_despesas}?schema=public
      NEXT_TELEMETRY_DISABLED: 1
    ports:
      - "3000:3000"
    volumes:
      - .:/app-node
      - /app-node/node_modules
    entrypoint: ["sh", "-c"]
    command: "npm install && npx prisma generate && npm run dev"
    networks:
      - sistema-despesas-net

networks:
  sistema-despesas-net:
    driver: bridge

volumes:
  postgres_data:
